# TODO normally when using tests without a database we should disable datasource context?

spring.datasource.url=jdbc:h2:mem:testdb;Mode=PostgreSQL
spring.datasource.username=sa
spring.datasource.password=